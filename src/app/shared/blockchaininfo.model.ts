export class BlockchainInfo {
    bestblockhash: string
    blocks: number
    chain: string
    chainwork: string
    difficulty: number
    headers: number
    mediantime: number
    pruned: boolean
}